var chai = require("chai");
chai.

describe('Geminid', function(){
  describe('#setUnreadCount()', function(){
    it('should something', function(){
      assert.equal(-1, [1,2,3].indexOf(5));
      assert.equal(-1, [1,2,3].indexOf(0));
    })
  })
})